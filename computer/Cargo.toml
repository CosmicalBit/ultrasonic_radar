[package]
name = "computer"
version = "0.1.0"
edition = "2024"

[dependencies]
raylib = "6.0.0"
tokio = { version = "1.53.1", features = ["macros", "net", "rt-multi-thread"] }
ultrasonic-radar = { path = ".." }
